# Supabase project (Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...   # anon/public key

# Optional: management endpoint URL for the agent (/health, /context, /dream)
# Chat delivery does NOT call the agent directly anymore.
NEXT_PUBLIC_AGENT_URL=

# Optional: token for management endpoint calls from the frontend
NEXT_PUBLIC_AGENT_TOKEN=

# Shared app config for agent identities shown in UI
AGENT_SHARED_PASSWORD=
LAB_256_EMAIL=lab-256@example.com
LAB_512_EMAIL=lab-512@example.com
LAB_8GB_EMAIL=lab-8gb@example.com
